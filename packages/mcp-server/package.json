{
  "name": "@vault-sync/mcp-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "deploy": "npm run build && aws lambda update-function-code --function-name ${MCP_FUNCTION:-vault-mcp-server} --zip-file fileb://dist/mcp-server.zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@vault-sync/core": "*",
    "@vault-sync/git-sync": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "fflate": "^0.8.2"
  }
}
